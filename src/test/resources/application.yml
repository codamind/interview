server:
  port: 9090

spring:
  application:
    name: ThorstenHagaInterview
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/lala
    username: testuser
    password: mypassword
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
